'''
    Prints a given grid, given its dimensions and the grid itself
'''
def printGrid(grid,r,c):
    gridStr = ""
    for i in range(r):
        curLine = ""
        for j in range(c):
            curLine += grid[i][j] + " "
        curLine.rstrip()
        gridStr += curLine + "\n"
    gridStr.rstrip()
    print gridStr
        
        
